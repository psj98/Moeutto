import TodayWeatherCard from '../molecules/TodayWeatherCard';

const ClothAnalysisOrganism = () => {
  return <TodayWeatherCard idx={1} />; // api 연결되면 idx는 동적으로 변함
};

export default ClothAnalysisOrganism;
